# ── Core vision & compute ─────────────────────────────────────────────────
opencv-python>=4.9.0
numpy>=1.26.0
torch>=2.2.0

# ── Qualcomm AI Hub – MediaPipe Hand landmark detector ────────────────────
# Provides the BlazePalm + BlazeHandLandmark pipeline for 21-joint hand
# skeleton extraction.  On ARM / Snapdragon, compile to .tflite / .onnx
# for NPU-accelerated inference.
qai-hub-models>=0.20.0

# ── Utilities ─────────────────────────────────────────────────────────────
Pillow>=10.0.0
